stages:
  - build
  - deploy

build:
  stage: build
  script:
    - npm install
    - mkdir -p build
    - npm run build
    - cp build/* /var/www/iiro.fi/

cache:
  paths:
    - node_modules
  key: "$CI_BUILD_REF"
  untracked: true
